{
  "scripts": {
    "lint": "eslint .",
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write .",
    "test": "jest --coverage",
    "build": "node scripts/build.js || echo Skipping build"
  }
}
